---
import Layout from "../layouts/Layout.astro";
import ActionsList from "../components/ActionsList.svelte";
import ActionItemsList from "../components/ActionItemsList.svelte";
---

<Layout header="Actions">
    <div class="actions">
        <ActionsList client:visible />
    </div>

    <ActionItemsList client:visible />
</Layout>

<style>
    .actions {
        position: absolute;
        right: 100%;
        width: 100%;
        max-width: calc(min(20ch, round(down, 100%, 1ch)));
    }
</style>
